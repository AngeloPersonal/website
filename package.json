{
  "name": "sanity-blog",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "dev:studio": "pnpm --filter=sanity-studio dev",
    "dev:next16": "pnpm --filter=frontend-next-16 dev",
    "dev": "concurrently \"pnpm dev:studio\" \"pnpm dev:next16\"",
    "types": "pnpm types:extract && pnpm types:generate",
    "types:extract": "pnpm --filter=sanity-studio types:extract",
    "types:generate": "pnpm --filter=sanity-studio types:generate",
    "types:watch": "pnpm --filter=sanity-studio types:watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.15.1",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
