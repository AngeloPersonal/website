services:
  sanity-studio:
    build: 
      context: ./src/studio
      target: production
    ports:
      # TODO: Make port configurable via environment
      - 3000:3333
    environment:
      - SANITY_PROJECT_ID=${SANITY_PROJECT_ID}